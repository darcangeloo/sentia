[pytest]
# Le funzioni pure del backend (parsing importi, deduplica, tokenizzazione,
# sanitizzazione, routing) sono testate senza rete né database. asyncio_mode
# auto permette di scrivere test async senza decoratore esplicito.
asyncio_mode = auto
testpaths = backend/tests
python_files = test_*.py
addopts = -q
